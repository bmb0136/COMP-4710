@startuml Pitch object
class Pitch{
    + toMidi(): int
    + fromMidi(int,boolen): Pitch
    + addSemitones(int): Pitch
    + addSemitones(int, boolean): Pitch
    + compareTo(Pitch): int
    + equals(Object): boolean
    + {static} tryParse(String): Optional<Pitch>
    }
@enduml